
// previous page
%a.prev

// root element for scrollable
.scrollable

  // root element for the items
  .items

    // THUMBNAILS 1-5
    %div

      - @material.images.each do |i|
        %a{:href => i.image.url(:large), name: "a1"}
          / (href="#" onMouseover="MM_swapImage('enlarge1','','images/stone/Amande/amande_4ptStalk.jpg',0); writeText('FINISH: Type of finish <br> and minimum thickness')")

          %img{:src => i.image.url(:thumb)}
          .close X
          / = link_to 'x', admin_image_path(i), :confirm => 'Are you sure you want to remove this image?', :method => :delete

        / %p= i.image_file_name


// next page
%a.next



// MAIN IMAGE
#enlarge_container
  %img{src: "images/stone/Amande/amande_4ptStalk.jpg", name: "enlarge1"}



